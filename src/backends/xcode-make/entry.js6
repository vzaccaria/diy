var debug = require('debug')('entry')

var {
    readTemplate
} = require('./readTemplate.es5.js')

var {
    fillWithData
} = require('./parseWithData.es5.js')

var {
    writeProject 
} = require('./writeProject.es5.js')


var _module = () => {

    var writeXcode = (data) => {
        var template = readTemplate();
        fillWithData(template, data).then((project) => {
            writeProject(project)
        })
    }

    return {
        writeXcode: writeXcode
    }
}

module.exports = _module()