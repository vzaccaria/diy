
var { cat, mkdir } = require('shelljs')
var { log }        = console
var debug = require('debug')('readTemplate')

var _module = () => {

    
    var readTemplate = () => {
    	var tt = `${__dirname}/../../../src/backends/xcode-make/template/MyProduct/MyProduct.xcodeproj/project.pbxproj`
        var curProjectPbx = cat(tt)
        return curProjectPbx;
    }


    return {
        readTemplate: readTemplate
    }

}

module.exports = _module()